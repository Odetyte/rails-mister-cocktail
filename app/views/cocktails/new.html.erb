<!-- create a new one -->

<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
      <h1>Add a new great cocktail</h1>
      <%= simple_form_for @cocktail do |f| %>
        <%= f.input :name %>
        <%= f.input :photo, as: :file %>
         <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
        <%= f.submit class: '"btn btn-outline-dark"' %>
      <% end %>
       <%= link_to "Back to all cocktails", root_path, class: "btn btn-outline-dark" %>
    </div>
  </div>
</div>


<!-- doses view from show/cocktails from doses view back show cocktail -->
<!-- f.asociation for ingredients -->
